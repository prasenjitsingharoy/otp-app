import { WebOtpDirective } from './web-otp.directive';

describe('WebOtpDirective', () => {
  it('should create an instance', () => {
    const directive = new WebOtpDirective();
    expect(directive).toBeTruthy();
  });
});
